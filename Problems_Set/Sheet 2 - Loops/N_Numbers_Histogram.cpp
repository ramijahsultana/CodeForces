#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define pb push_back
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    char s;
    cin >> s;
    int n;
    cin >> n;
    while (n--)
    {
        int x;
        cin >> x;
        for (int i = 0; i < x; i++)
        {
            cout << s;
        }
        cout << "\n";
    }

    return 0;
}
